# Copy to .env (development) or .env.production (build artifacts) and edit.
# scripts/set-env.js loads .env first, then overlays .env.<NODE_ENV>.
# Never commit real .env files - they are git-ignored.

NODE_ENV=development
API_URL=http://localhost:3010
